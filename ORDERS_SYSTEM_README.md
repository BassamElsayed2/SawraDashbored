# نظام إدارة الطلبات - لوحة التحكم

تم إضافة نظام كامل لإدارة الطلبات في لوحة التحكم (Dashboard).

## الملفات المضافة

### 1. Service Layer

- **`services/apiOrders.ts`**: يحتوي على جميع الـ API calls للطلبات
  - `getAllOrders()`: جلب جميع الطلبات مع إمكانية التصفية
  - `getOrderById()`: جلب تفاصيل طلب واحد
  - `updateOrderStatus()`: تحديث حالة الطلب
  - `getOrderStatistics()`: جلب إحصائيات الطلبات
  - `updateOrderNotes()`: تحديث ملاحظات الطلب
  - `assignOrderToBranch()`: تعيين الطلب لفرع معين

### 2. Components

- **`src/components/Orders/OrdersStats.tsx`**: عرض إحصائيات الطلبات

  - إجمالي الطلبات
  - الطلبات حسب الحالة (قيد الانتظار، تم التوصيل، ملغاة، إلخ)
  - إجمالي الإيرادات
  - متوسط قيمة الطلب
  - طلبات التوصيل vs الاستلام

- **`src/components/Orders/OrdersFilters.tsx`**: فلترة الطلبات

  - تصفية حسب الحالة
  - تصفية حسب نوع الطلب (توصيل/استلام)
  - تصفية حسب الفرع
  - تصفية حسب التاريخ (من - إلى)

- **`src/components/Orders/OrdersTable.tsx`**: جدول عرض الطلبات
  - عرض قائمة الطلبات
  - تغيير حالة الطلب مباشرة من الجدول
  - رابط لعرض تفاصيل كل طلب

### 3. Pages

- **`src/app/(protected)/dashboard/orders/page.tsx`**: صفحة قائمة الطلبات الرئيسية

  - عرض جميع الطلبات
  - الإحصائيات
  - الفلاتر
  - الجدول

- **`src/app/(protected)/dashboard/orders/[id]/page.tsx`**: صفحة تفاصيل الطلب
  - تفاصيل الطلب الكاملة
  - المنتجات المطلوبة
  - عنوان التوصيل
  - تحديث حالة الطلب
  - إضافة/تعديل ملاحظات
  - معلومات المستخدم

### 4. Sidebar Menu

- تم تحديث **`src/components/Layout/SidebarMenu/index.tsx`** لإضافة قسم "الطلبات" في القائمة الجانبية

## حالات الطلب (Order Status)

النظام يدعم الحالات التالية للطلب:

1. **pending** (قيد الانتظار): الطلب الجديد
2. **confirmed** (مؤكد): تم تأكيد الطلب
3. **preparing** (قيد التحضير): جاري تحضير الطلب
4. **ready** (جاهز): الطلب جاهز للاستلام/التوصيل
5. **delivering** (قيد التوصيل): جاري توصيل الطلب
6. **delivered** (تم التوصيل): تم توصيل الطلب بنجاح
7. **cancelled** (ملغى): تم إلغاء الطلب

## الميزات الرئيسية

### 1. لوحة الإحصائيات

- عرض شامل لجميع الإحصائيات المهمة
- تحديث تلقائي عند تغيير الفلاتر
- بطاقات ملونة لسهولة القراءة

### 2. نظام التصفية المتقدم

- تصفية متعددة المعايير
- إمكانية إعادة تعيين الفلاتر
- تحديث فوري للنتائج

### 3. إدارة الطلبات

- تحديث حالة الطلب بسهولة
- عرض تفاصيل شاملة لكل طلب
- إضافة ملاحظات للطلبات
- عرض معلومات التوصيل والعنوان

### 4. واجهة المستخدم

- تصميم متجاوب يعمل على جميع الأجهزة
- دعم الوضع الداكن (Dark Mode)
- رسائل تأكيد للعمليات (Toast Notifications)
- حالات التحميل (Loading States)

## كيفية الاستخدام

### الوصول إلى نظام الطلبات

1. قم بتسجيل الدخول إلى لوحة التحكم
2. من القائمة الجانبية، اضغط على "الطلبات"
3. اضغط على "قائمة الطلبات"

### عرض تفاصيل طلب

1. من صفحة قائمة الطلبات
2. اضغط على "عرض التفاصيل" بجانب الطلب المراد
3. ستظهر جميع تفاصيل الطلب

### تحديث حالة الطلب

يمكن تحديث حالة الطلب بطريقتين:

1. **من الجدول**: استخدم القائمة المنسدلة في عمود "تغيير الحالة"
2. **من صفحة التفاصيل**: اضغط على الحالة المطلوبة من القائمة الجانبية

### تصفية الطلبات

1. استخدم قسم "تصفية الطلبات" في أعلى الصفحة
2. اختر المعايير المطلوبة (الحالة، النوع، الفرع، التاريخ)
3. سيتم تحديث القائمة تلقائياً

### إضافة ملاحظات

1. افتح صفحة تفاصيل الطلب
2. في قسم "ملاحظات الطلب"، اكتب الملاحظات
3. اضغط على "تحديث الملاحظات"

## متطلبات قاعدة البيانات

تأكد من تنفيذ ملف SQL التالي في قاعدة البيانات:

- `app/supabase_orders_table.sql`

هذا الملف يحتوي على:

- إنشاء جدول `orders`
- إنشاء الفهارس (Indexes) للأداء الأفضل
- إعداد Row Level Security (RLS)
- إنشاء Triggers للتحديث التلقائي

## الأمان (Security)

- جميع العمليات تتطلب تسجيل دخول المسؤول
- يتم التحقق من الصلاحيات قبل كل عملية
- Row Level Security (RLS) مفعل على جدول الطلبات
- جميع البيانات الحساسة محمية

## التطوير المستقبلي

يمكن تطوير النظام بإضافة:

- إشعارات فورية عند استلام طلب جديد
- تصدير البيانات إلى Excel/PDF
- نظام طباعة الطلبات
- تتبع حالة الطلب في الوقت الفعلي
- تحليلات متقدمة ورسوم بيانية
- نظام تعيين الطلبات للموظفين
- تقارير مفصلة عن الطلبات

## دعم فني

في حالة وجود أي مشاكل أو استفسارات، يرجى التواصل مع فريق التطوير.
